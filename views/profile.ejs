<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Профиль пользователя</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f7f7f7;
        }
        .container {
            background: #fff;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,.1);
            max-width: 700px;
            margin: auto;
        }
        .user-info img {
            border-radius: 50%;
            border: 3px solid #ddd;
            width: 150px;
            height: 150px;
            object-fit: cover; /* Обеспечивает, чтобы изображение нормально вписывалось в контейнер */
        }
        .user-info {
            margin-bottom: 30px;
        }
        .btn-primary {
            width: 100%;
        }
        .form-group label {
            font-weight: bold;
        }
        .form-control, .btn-primary, .form-control-file {
            margin-top: 15px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="text-center mb-4">
            <h2>Профиль пользователя</h2>
        </div>
        <!-- Отображение информации о пользователе -->
        <div class="user-info text-center">
            <!-- Предполагаем, что user.photo содержит относительный путь от корня сервера -->
            <img src="<%= user.photo ?  user.photo : '/img/profile.png' %>" alt="Фото профиля">
            <h3 class="mt-3"><%= user.name %></h3>
            <p>Возраст: <%= user.age %></p>
            <p class="mb-3">О себе: <%= user.description %></p>
        </div>
        
        <!-- Форма для обновления профиля -->
        <form action="/update-profile" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="name">Имя:</label>
                <input type="text" class="form-control" id="name" name="name" value="<%= user.name %>" required>
            </div>
            <div class="form-group">
                <label for="age">Возраст:</label>
                <input type="number" class="form-control" id="age" name="age" value="<%= user.age %>" required>
            </div>
            <div class="form-group">
                <label for="description">О себе:</label>
                <textarea class="form-control" id="description" name="description" rows="3" required><%= user.description %></textarea>
            </div>
            <div class="form-group">
                <label for="photo">Фотография:</label>
                <input type="file" class="form-control-file" id="photo" name="photo">
            </div>
            <button type="submit" class="btn btn-primary">Обновить профиль</button>
        </form>
    </div>

    <!-- Bootstrap JS и зависимости -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
